import { LoadingState } from '@/shared/ui/state/LoadingState';

export default function Loading() {
  return <LoadingState title="Cargando backoffice" description="Preparando el dashboard operativo." />;
}
